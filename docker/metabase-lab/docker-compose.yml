services:
  metabase:
    image: metabase/metabase:latest
    container_name: metabase
    ports:
      - "3000:3000"
    networks:
      - docker-hive_default

networks:
  docker-hive_default:
    external: true
